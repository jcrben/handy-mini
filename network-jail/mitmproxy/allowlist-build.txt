# network-jail outbound allowlist for the Handy BUILD phase
# Format: one entry per line. Either:
#   - hostname            (matches any port)
#   - hostname:port       (exact host+port)
#   - *.suffix.example    (wildcard subdomain)
#
# All matches are case-insensitive. See docs/expected-egress.md for rationale.

# --- Rust / cargo ---
index.crates.io
static.crates.io
crates.io

# --- cargo git deps + bun binary release fetch + AppImage tooling ---
github.com
codeload.github.com
objects.githubusercontent.com

# --- JavaScript / bun ---
registry.npmjs.org
bun.sh

# --- ONNX Runtime native binaries (ort-rs crate fetches prebuilt libs here) ---
# ort-rs (pykeio/ort) is the Rust binding for ONNX Runtime. Its `download`
# strategy fetches a precompiled MS ONNX Runtime tarball from pyke.io's CDN.
# Used transitively by transcribe-rs (Parakeet / Moonshine inference).
cdn.pyke.io

# --- TLS bootstrap helper for mitmproxy CA install ---
mitm.it
